{
  "name": "creed",
  "version": "1.0.0",
  "description": "Forward-looking async toolkit: promises, iterators, generators, streaming",
  "main": "lib/Promise.js",
  "files": [
    "lib",
    "dist"
  ],
  "scripts": {
    "build": "babel -q -d lib src && webpack --config .webpack.js && uglifyjs dist/creed.js -c 'warnings=false' -m --source-map=dist/creed.min.js.map --source-map-url=creed.min.js.map --source-map-include-sources -o dist/creed.min.js",
    "test": "jscs -x src && npm run build && promises-aplus-tests test/aplus.js"
  },
  "author": "brian@hovercraftstudios.com",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.6.23",
    "jscs": "^1.13.1",
    "mocha": "^2.2.5",
    "node-libs-browser": "^0.5.2",
    "uglifyjs": "^2.4.10",
    "webpack": "^1.10.1"
  }
}
