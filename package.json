{
  "name": "truth",
  "version": "1.0.0",
  "description": "",
  "main": "truth.js",
  "scripts": {
    "build": "babel -d lib src && browserify -o build/truth.js -s truth --no-detect-globals truth.js && uglifyjs build/truth.js -c -m --source-map=build/truth.min.js.map --source-map-url=truth.min.js.map --source-map-include-sources -o build/truth.min.js",
    "test": "npm run build && promises-aplus-tests test/aplus.js"
  },
  "author": "brian@hovercraftstudios.com",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.5.8",
    "browserify": "^10.2.4",
    "mocha": "^2.2.5"
  }
}
