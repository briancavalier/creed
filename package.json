{
  "name": "truth",
  "version": "1.0.0",
  "description": "Forward-looking async toolkit: promises, generators, streaming",
  "main": "lib/Promise.js",
  "files": [
    "lib",
    "dist"
  ],
  "scripts": {
    "build": "babel -d lib src && browserify -o dist/truth.js -s truth --no-detect-globals lib/Promise.js && uglifyjs dist/truth.js -c -m --source-map=dist/truth.min.js.map --source-map-url=truth.min.js.map --source-map-include-sources -o dist/truth.min.js",
    "test": "npm run build && promises-aplus-tests test/aplus.js"
  },

  "author": "brian@hovercraftstudios.com",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.5.8",
    "browserify": "^10.2.4",
    "mocha": "^2.2.5"
  }
}
